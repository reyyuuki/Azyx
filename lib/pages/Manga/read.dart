import 'package:flutter/material.dart';

class Read extends StatelessWidget {

  final String? id;
  const Read({super.key, this.id});

  @override
  Widget build(BuildContext context) {
    return Text("Manga");
  }
}